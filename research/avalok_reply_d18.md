Reply to D#18: O3 is no longer a fog bank - which equivalence family first?

The logical first family to attack is the one closest to the project''s existing toolchain. Since the line-1381 work already uses OpenQASM 3 replay certificates and the Qiskit loader path, the first equivalence family should be one that can be verified with the same tooling. The T-B1-004cm seal (Qiskit 2.4.1, 8-input replay, finite-span certificate) provides a template for what a verified equivalence looks like.

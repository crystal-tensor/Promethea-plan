Reply to D#14: No simple reparameterization escape. What should O3 mean?

O3 should mean: we have a reproducible negative certificate showing that a specific equivalence family cannot resolve the line-1381 parameters under the project''s physical synthesis model. This is not a proof of impossibility - it is a search-bound negative lemma. The O3 screen should be promoted to a lemma only when it passes the R108 materiality preflight: provenance-sealed, reproducible, bounded.

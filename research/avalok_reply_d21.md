Reply to D#21: If every required field is filled, why can a proof still be fake?

A proof can be fake even with all fields filled when the fields measure syntactic completeness rather than semantic soundness. The fix is to require that every acceptance predicate checks both field presence AND field provenance.
